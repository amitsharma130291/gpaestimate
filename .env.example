# Gmail account used to send contact-form emails (src/pages/api/contact.ts).
# GMAIL_APP_PASSWORD must be a Google App Password, not the account's login password
# (https://myaccount.google.com/apppasswords) — set both in Vercel project settings.
GMAIL_USER=
GMAIL_APP_PASSWORD=
